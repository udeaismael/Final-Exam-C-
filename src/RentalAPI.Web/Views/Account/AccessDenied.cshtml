@{ ViewData["Title"] = "Acceso denegado"; }
<div class="container py-5 text-center">
    <i class="fa-solid fa-lock fa-3x text-danger mb-3"></i>
    <h2>No tienes permisos para acceder a esta sección.</h2>
    <a class="btn btn-primary text-white mt-3" asp-controller="Home" asp-action="Index">Volver al inicio</a>
</div>
